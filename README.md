git --version

git init

git add .

git add index.html

git commit -m "Initial commit"

git branch -M main

git remote remove origin

git remote add origin https://github.com/Shahibullah/HTML-Projects.git

git remote -v

git push -u origin main

--->Next time (after first push):

git push
